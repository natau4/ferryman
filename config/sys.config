[
    {kernel,
        [
            {logger,
                [{handler, default, logger_std_h,
                    #{
                        level => debug,
                        config => #{type => {file,"/tmp/ferryman.log"}}
                    }
                }]
            },
            {logger_level, debug}
        ]
    }
].